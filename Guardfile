guard :coffeescript, output: 'public/js/gen' do
  watch(%r{^src/coffeescripts/(.*)\.coffee})
end

guard 'compass' do
watch(%r{(.*)\.s[ac]ss$})
end


guard 'livereload' do
watch(%r{.+\.(css|js|html?|jade)$})
end
